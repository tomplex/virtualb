Create a requirements.txt file for a virtualenv.

Usage:
    vb requires <virtualenv=current> [file name=requirements.txt]

Create a requirements.txt file for the current
or specified environment, using the specified
filepath, defaulting to $PWD/requirements.txt.

